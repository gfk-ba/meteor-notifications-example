#!/usr/bin/expect

set timeout 1000
set email "#EMAIL#"
set password "#PASSWORD#"

spawn meteor deploy #SITENAME#

#puts $command
expect -- "*Email:"
send "$email\n"
expect -- "*assword:"
send -- "$password\n"
expect -- "*Now serving at #SITENAME#"
